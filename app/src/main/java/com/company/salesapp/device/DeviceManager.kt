package com.company.salesapp.device

/**
 * TODO Phase H (section 26) — Device Registration.
 * Kirim device_id, device_name, app_version, platform, status, last_seen
 * ke Laravel saat login / start work. Jangan gunakan hardware identifier
 * yang tidak diperlukan (section 26).
 */
class DeviceManager {
    // TODO: implementasikan endpoint registrasi device di ApiService, panggil saat login sukses.
}
