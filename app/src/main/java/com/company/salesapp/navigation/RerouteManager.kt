package com.company.salesapp.navigation

/**
 * TODO Phase F (section 22) — Re-routing.
 * Re-route HANYA saat: deviation signifikan, destination berubah, route invalid,
 * navigation dimulai, atau sistem menentukan perlu recalculation.
 * GPS update biasa TIDAK memicu pemanggilan TomTom berulang.
 */
class RerouteManager {
    // TODO: deteksi deviation dari jarak titik GPS ke polyline aktif,
    // panggil RouteManager untuk request route baru bila melewati threshold.
}
