package com.company.salesapp.navigation

/**
 * TODO Phase F (section 32) — Navigation Mode (in-app, section 21).
 * Menampilkan: destination, distance, ETA, current GPS, next maneuver, route.
 * Navigasi TETAP di dalam APK — Google Maps App eksternal tidak digunakan (section 35 no.2).
 */
class NavigationManager {
    // TODO: hubungkan dengan AppLocationManager (lokasi live) + RouteManager (route aktif)
}
